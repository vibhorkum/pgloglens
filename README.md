# pgloglens
pgLogLens is a PostgreSQL log analyzer that turns raw logs into clear operational insight. It helps identify errors, slow queries, lock contention, connection issues, and performance patterns quickly.
